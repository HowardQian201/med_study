### Start frontend
cd client
npm run dev

### Start backend
.venv\Scripts\activate
python devapp.py

### Start background worker
.venv\Scripts\activate
celery -A backend.background.worker worker --loglevel=debug --pool=solo

### dev redis
Start in docker desktop
docker run --name medstudy-redis -p 6379:6379 -d redis
docker ps # check if running
docker exec -it medstudy-redis redis-cli
FLUSHALL


### if terminal is stuck
Get-Process python, uvicorn -ErrorAction SilentlyContinue | Stop-Process -Force
